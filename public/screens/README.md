# Screens

Source PNGs at `D:/MyProjects/github_profile/OttomanDeveloper/Assets/` (not committed here).

To re-convert, run:

```
node scripts/convert-screens.mjs
```

Output: 540px-wide AVIF (quality 60, effort 6). Target size: ≤80 KB per file.

## Aliases used by the site

- `hero-front.avif`, `hero-mid.avif`, `hero-back.avif` — the 3 phones in the hero stack.
- `chronos.avif`, `piggytoken.avif` — placeholders. Replace with real captures of the live demos before deploy.
